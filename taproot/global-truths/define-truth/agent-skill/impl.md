# Implementation: Agent Skill

## Behaviour
../usecase.md

## Design Decisions
- **Skill-only implementation**: the entire define-truth flow is agent-driven prose. No TypeScript CLI command is needed — creating a markdown file in `taproot/global-truths/` is a trivial file-write the agent performs directly.
- **Pre-populated context path**: the skill accepts an optional `candidate` argument so that `/tr-discover-truths` and `/tr-ineed` can route promoted candidates here with term, scope, and evidence pre-filled. When no argument is provided, the skill asks interactively.
- **Scope determination is interactive**: the skill asks the developer to confirm scope rather than inferring it, to avoid silently applying the wrong scope to an important truth.
- **Both conventions supported equally**: the skill offers suffix and sub-folder as equal options and creates the file accordingly — no preference encoded.
- **Scope ambiguity flagged inline**: when a developer writes a truth without a clear scope signal, the skill surfaces the warning as part of the completion summary rather than as a blocker.

## Source Files
- `skills/define-truth.md` — skill implementing the full define-truth flow (AC-1 through AC-7, all alternate flows and error conditions)
- `.taproot/skills/define-truth.md` — synced copy for agent use
- `.claude/commands/tr-define-truth.md` — Claude adapter (generated by taproot update)
- `src/commands/init.ts` — `define-truth.md` added to `SKILL_FILES`

## Commits
- (run `taproot link-commits` to populate)

## Tests
- `test/integration/define-truth.test.ts` — covers AC-1 (suffix convention), AC-2 (sub-folder convention), AC-5 (unscopable file defaults to intent with warning), AC-6 (conflicting scope signals), and structural requirements (skill exists, has description ≤50 tokens, has What's next?, synced to .taproot/skills/)

## DoR Resolutions
- condition: check-if-affected-by: quality-gates/architecture-compliance | note: reviewed architecture-compliance spec; this implementation adds one skill file and one test. No TypeScript source changes beyond SKILL_FILES registration. No architectural deviations. | resolved: 2026-03-26
- condition: check-if-affected-by: quality-gates/nfr-measurability | note: no new NFRs introduced; the skill is interactive agent prose with no performance constraints. | resolved: 2026-03-26

## Status
- **State:** in-progress
- **Created:** 2026-03-26
- **Last verified:** 2026-03-26
