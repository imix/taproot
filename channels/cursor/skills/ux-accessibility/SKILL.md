---
name: "tr-ux-accessibility"
description: "Define accessibility UX conventions: keyboard model, focus management, contrast targets, motion preferences, labelling, and live regions"
---

Load and follow the full skill definition at `taproot/agent/skills/ux-accessibility.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
