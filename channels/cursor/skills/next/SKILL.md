---
name: "tr-next"
description: "Surface the next independently-implementable work item from the requirement hierarchy"
---

Load and follow the full skill definition at `taproot/agent/skills/next.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
