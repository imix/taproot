---
name: "tr-status"
description: "Generate a health dashboard for the requirement hierarchy"
---

Load and follow the full skill definition at `taproot/agent/skills/status.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
