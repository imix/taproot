---
name: "tr-explore"
description: "Collaborative requirement exploration — think through a vague idea before speccing it; asks one focused question at a time and hands off to the right skill when ready"
---

Load and follow the full skill definition at `taproot/agent/skills/explore.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
