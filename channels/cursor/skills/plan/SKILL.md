---
name: "tr-plan"
description: "Build or modify the persistent implementation plan (`taproot/plan.md`)"
---

Load and follow the full skill definition at `taproot/agent/skills/plan.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
