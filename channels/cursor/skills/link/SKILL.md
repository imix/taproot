---
name: "tr-link"
description: "Create a cross-repo link file (`link.md`) that references a spec — intent, behaviour, or global truth — in another repository"
---

Load and follow the full skill definition at `taproot/agent/skills/link.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
