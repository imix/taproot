---
name: "tr-promote"
description: "Escalate a significant finding from implementation or behaviour level up to the intent"
---

Load and follow the full skill definition at `taproot/agent/skills/promote.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
