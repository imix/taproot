---
name: "tr-backlog"
description: "Capture ideas, findings, and deferred work mid-session with a single command — without interrupting current flow"
---

Load and follow the full skill definition at `taproot/agent/skills/backlog.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
