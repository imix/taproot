---
name: "tr-bug"
description: "Diagnose a defect through structured root cause analysis (5-Why) and delegate to the right fix skill"
---

Load and follow the full skill definition at `taproot/agent/skills/bug.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
