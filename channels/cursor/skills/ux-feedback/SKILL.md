---
name: "tr-ux-feedback"
description: "Define feedback UX conventions: success/error/warning hierarchy, loading states, and partial-outcome handling"
---

Load and follow the full skill definition at `taproot/agent/skills/ux-feedback.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
