---
name: "tr-ux-presentation"
description: "Define presentation UX conventions: layout structure, hierarchy signals, density, progressive disclosure, and collection display"
---

Load and follow the full skill definition at `taproot/agent/skills/ux-presentation.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
