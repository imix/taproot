---
name: "tr-audit-all"
description: "Run a comprehensive review of an entire subtree — an intent and all its descendants, or the entire `taproot/` root"
---

Load and follow the full skill definition at `taproot/agent/skills/audit-all.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
