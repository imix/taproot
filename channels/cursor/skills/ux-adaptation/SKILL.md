---
name: "tr-ux-adaptation"
description: "Define adaptation UX conventions: environment targets, layout reflow, dark/high-contrast support, and constrained-environment fallbacks"
---

Load and follow the full skill definition at `taproot/agent/skills/ux-adaptation.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
