---
name: "tr-ux-input"
description: "Define input UX conventions: validation timing, required/optional signalling, defaults, and keyboard affordances"
---

Load and follow the full skill definition at `taproot/agent/skills/ux-input.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
