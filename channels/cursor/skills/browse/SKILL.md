---
name: "tr-browse"
description: "Read a taproot hierarchy document section by section in the terminal — without opening an external editor"
---

Load and follow the full skill definition at `taproot/agent/skills/browse.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
