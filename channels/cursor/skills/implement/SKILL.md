---
name: "tr-implement"
description: "Implement a behaviour spec: write the code, write the tests, create the `impl.md` traceability record, and commit with the conventional tag format"
---

Load and follow the full skill definition at `taproot/agent/skills/implement.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
