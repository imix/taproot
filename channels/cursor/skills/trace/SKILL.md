---
name: "tr-trace"
description: "Navigate the requirement hierarchy in any direction: from code to intent (bottom-up), from intent to code (top-down), laterally across siblings, or scan for unlinked code"
---

Load and follow the full skill definition at `taproot/agent/skills/trace.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
