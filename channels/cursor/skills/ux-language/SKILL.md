---
name: "tr-ux-language"
description: "Define language UX conventions: copy tone, terminology, locale handling, variable-length text, and formatting"
---

Load and follow the full skill definition at `taproot/agent/skills/ux-language.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
