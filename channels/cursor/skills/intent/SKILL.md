---
name: "tr-intent"
description: "Create a new business intent or refine an existing one"
---

Load and follow the full skill definition at `taproot/agent/skills/intent.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
