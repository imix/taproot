---
name: "tr-refine"
description: "Update a behaviour spec (`usecase.md`) based on what was learned during or after implementation"
---

Load and follow the full skill definition at `taproot/agent/skills/refine.md` in the current workspace.

Follow every step in the skill's `## Steps` section precisely and in order. Use the `cli:` value from `taproot/settings.yaml` for all taproot CLI commands (default: `taproot`).
